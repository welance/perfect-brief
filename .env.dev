# develop env (r001-15.develop.welance.space) — mock judge only, no secrets.
# CI wraps this file into the develop tenant's env.yaml on each deploy.
PB_DEFAULT_JUDGE=mock
PB_REDIS_URL=redis://redis:6379/0
